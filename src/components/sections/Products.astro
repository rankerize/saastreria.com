---
import { products } from '../../data/services';
---
<section id="productos" class="py-24 px-6">
  <div class="max-w-7xl mx-auto">
    <div class="text-center mb-16">
      <span class="inline-block px-3 py-1 rounded-full border border-[#F59E0B]/30 bg-[#F59E0B]/10 text-[#F59E0B] text-xs font-medium mb-4">
        Productos propios
      </span>
      <h2 class="text-3xl sm:text-4xl font-bold mb-4" style="font-family: 'Plus Jakarta Sans', sans-serif;">
        Software listo para usar hoy
      </h2>
      <p class="text-[#94A3B8] max-w-xl mx-auto">
        Además de desarrollar soluciones a medida, diseñamos productos configurables para necesidades recurrentes.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-8">
      {products.map((p, i) => (
        <div class="glass rounded-2xl overflow-hidden hover:border-[#F59E0B]/20 transition-all hover:shadow-[0_0_40px_rgba(245,158,11,0.08)]">
          <!-- Product header -->
          <div class="p-8 pb-6">
            <div class="flex items-start justify-between mb-4">
              <span class="inline-block px-2 py-0.5 rounded-full bg-green-500/10 border border-green-500/20 text-green-400 text-xs font-medium">
                {p.badge}
              </span>
            </div>
            <h3 class="text-xl font-bold text-white mb-1" style="font-family: 'Plus Jakarta Sans', sans-serif;">{p.name}</h3>
            <p class="text-[#F59E0B] text-sm font-medium mb-4">{p.tagline}</p>
            <p class="text-[#94A3B8] text-sm leading-relaxed">{p.description}</p>
          </div>

          <!-- Features -->
          <div class="px-8 pb-6">
            <ul class="space-y-2">
              {p.features.map(f => (
                <li class="flex items-start gap-2.5 text-sm text-[#94A3B8]">
                  <svg class="w-4 h-4 text-[#6366F1] flex-shrink-0 mt-0.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                    <path d="M20 6L9 17l-5-5"/>
                  </svg>
                  {f}
                </li>
              ))}
            </ul>
          </div>

          <!-- CTA -->
          <div class="px-8 pb-8">
            <a
              href={p.href}
              class="inline-flex items-center gap-2 w-full justify-center px-5 py-3 rounded-xl border border-[#F59E0B]/30 text-[#F59E0B] hover:bg-[#F59E0B]/10 font-medium text-sm transition-all"
            >
              Conocer producto
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
            </a>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
