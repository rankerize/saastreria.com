---
const steps = [
  {
    n: '01', title: 'Diagnóstico',
    color: '#6366F1',
    desc: 'Entendemos la operación, los sistemas y los objetivos reales del negocio antes de escribir una sola línea de código.',
    detail: 'Sesión de diagnóstico gratuita · mapeo de procesos · identificación del punto de partida ideal',
    icon: `<path d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>`,
  },
  {
    n: '02', title: 'Datos',
    color: '#8B5CF6',
    desc: 'Centralizamos y estructuramos las fuentes de información dispersas en una arquitectura de datos limpia y gobernada.',
    detail: 'Data warehouse · pipelines · calidad de datos · fuentes unificadas',
    icon: `<ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"/><path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/>`,
  },
  {
    n: '03', title: 'Software',
    color: '#EC4899',
    desc: 'Diseñamos y construimos el sistema a la medida del proceso real. Sin código genérico, sin plantillas, sin compromisos.',
    detail: 'Aplicaciones web · APIs · módulos empresariales · integración de sistemas legacy',
    icon: `<polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/>`,
  },
  {
    n: '04', title: 'Inteligencia',
    color: '#F59E0B',
    desc: 'Incorporamos IA y automatización inteligente donde genera valor real y demostrable para el negocio.',
    detail: 'RAG empresarial · agentes de IA · análisis predictivo · Text-to-SQL',
    icon: `<path d="M12 2a8 8 0 018 8c0 3.5-2 6.5-5 8l-3 .9-3-.9c-3-1.5-5-4.5-5-8a8 8 0 018-8z"/><circle cx="12" cy="10" r="3"/>`,
  },
  {
    n: '05', title: 'Flujos',
    color: '#10B981',
    desc: 'Conectamos procesos y sistemas para que la operación funcione de forma integrada, sin fricción ni intervención manual.',
    detail: 'Automatización de procesos · workflows · notificaciones · triggers automáticos',
    icon: `<path d="M22 12h-4l-3 9L9 3l-3 9H2"/>`,
  },
  {
    n: '06', title: 'Control',
    color: '#06B6D4',
    desc: 'Monitoreo en tiempo real, alertas y gobierno de datos para que nada quede sin visibilidad ni trazabilidad.',
    detail: 'Dashboards de control · alertas · logs · auditoría · SLAs',
    icon: `<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="m9 12 2 2 4-4"/>`,
  },
  {
    n: '07', title: 'Reportes',
    color: '#818CF8',
    desc: 'Decisiones basadas en datos reales en tiempo real. No en Excel del mes pasado, no en intuición.',
    detail: 'BI empresarial · reportes automáticos · KPIs en tiempo real · visualización ejecutiva',
    icon: `<line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/>`,
  },
];

const stepsJson = JSON.stringify(steps);
---

<section class="py-28 px-6 relative overflow-hidden" style="background:#0D0D1F;">

  <!-- Glow ambiental -->
  <div class="absolute inset-0 pointer-events-none" aria-hidden="true">
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[900px] h-[500px] rounded-full opacity-60"
         style="background:radial-gradient(ellipse,rgba(99,102,241,0.07) 0%,transparent 70%);"></div>
  </div>

  <div class="max-w-6xl mx-auto relative z-10">

    <!-- Header -->
    <div class="text-center mb-20 scroll-reveal">
      <span class="eyebrow mb-6">Nuestra metodología</span>
      <h2 class="text-4xl sm:text-5xl font-extrabold mt-6 mb-5 leading-[1.1]" style="font-family:'Plus Jakarta Sans',sans-serif;">
        Del diagnóstico al resultado.<br/>
        <span class="gradient-text">Un proceso claro y medible.</span>
      </h2>
      <p class="text-[#94A3B8] max-w-xl mx-auto text-lg">
        Seguimos siete pasos que transforman la operación. Nada de genéricos — cada fase se adapta al negocio real.
      </p>
    </div>

    <!-- ══ TRACK + PANEL ANIMADO ══ -->
    <div class="scroll-reveal">

      <!-- Track de pasos -->
      <div class="relative flex items-start justify-between mb-10 px-4" id="step-track">

        <!-- Línea de fondo -->
        <div class="absolute left-[5%] right-[5%] h-0.5 rounded-full top-9 z-0" style="background:rgba(255,255,255,0.06);"></div>
        <!-- Línea de progreso -->
        <div id="track-progress" class="absolute left-[5%] h-0.5 rounded-full top-9 z-0"
             style="width:0%;background:linear-gradient(90deg,#6366F1,#8B5CF6,#EC4899,#F59E0B,#10B981,#06B6D4,#818CF8);transition:width 0.6s cubic-bezier(0.16,1,0.3,1);"></div>

        <!-- Step circles -->
        {steps.map((s, i) => (
          <button
            class="step-btn flex flex-col items-center gap-2.5 relative z-10 group/step flex-1"
            data-idx={i}
            aria-label={`Paso ${s.n}: ${s.title}`}
          >
            <!-- Círculo -->
            <div
              class="step-circle w-[68px] h-[68px] rounded-2xl flex flex-col items-center justify-center border-2 transition-all duration-500 relative mx-auto"
              style={`background:${s.color}12;border-color:${s.color}30;`}
              data-color={s.color}
            >
              <!-- Número -->
              <span class="text-[9px] font-black font-mono tracking-widest mb-1 opacity-50" style={`color:${s.color};`}>{s.n}</span>
              <!-- Icono -->
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none"
                   stroke={s.color} stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"
                   set:html={s.icon}/>
              <!-- Anillo de glow (activo) -->
              <div class="step-ring absolute inset-0 rounded-2xl opacity-0 transition-all duration-500"
                   style={`box-shadow:0 0 0 3px ${s.color}60, 0 0 30px ${s.color}40;`}></div>
            </div>
            <!-- Título -->
            <span class="step-label text-[11px] font-semibold transition-all duration-300 text-[#475569]"
                  style="font-family:'Plus Jakarta Sans',sans-serif;">{s.title}</span>
          </button>
        ))}
      </div>

      <!-- Panel featured del paso activo -->
      <div class="relative rounded-3xl overflow-hidden" id="step-panel"
           style="background:rgba(255,255,255,0.025);border:1px solid rgba(255,255,255,0.07);">

        <!-- Glow de color del paso activo -->
        <div id="panel-glow" class="absolute inset-0 pointer-events-none transition-all duration-700 opacity-30"
             style="background:radial-gradient(ellipse at 50% 0%,rgba(99,102,241,0.3) 0%,transparent 60%);"></div>

        <!-- Contenido animado -->
        <div id="step-content" class="relative z-10 p-10 sm:p-14 flex flex-col sm:flex-row gap-10 items-center">

          <!-- Icono grande -->
          <div id="panel-icon-wrap" class="flex-shrink-0 w-[96px] h-[96px] rounded-3xl flex items-center justify-center border-2 transition-all duration-700"
               style="background:rgba(99,102,241,0.12);border-color:rgba(99,102,241,0.35);">
            <svg id="panel-icon" width="44" height="44" viewBox="0 0 24 24" fill="none"
                 stroke="#6366F1" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
            </svg>
          </div>

          <!-- Texto -->
          <div class="flex-1 min-w-0">
            <!-- Número + total -->
            <div class="flex items-center gap-3 mb-3">
              <span id="panel-num" class="text-xs font-black font-mono tracking-widest" style="color:#6366F1;">PASO 01 DE 07</span>
              <!-- Dots navegación -->
              <div class="flex items-center gap-1.5 ml-auto" id="nav-dots">
                {steps.map((_, i) => (
                  <div class="nav-dot rounded-full transition-all duration-300" data-idx={i}
                       style="width:6px;height:6px;background:rgba(255,255,255,0.15);"></div>
                ))}
              </div>
            </div>
            <h3 id="panel-title" class="text-3xl font-extrabold text-white mb-3"
                style="font-family:'Plus Jakarta Sans',sans-serif;">Diagnóstico</h3>
            <p id="panel-desc" class="text-[#94A3B8] text-base leading-relaxed mb-4">
              Entendemos la operación, los sistemas y los objetivos reales del negocio antes de escribir una sola línea de código.
            </p>
            <p id="panel-detail" class="text-sm font-medium" style="color:#6366F1;">
              Sesión de diagnóstico gratuita · mapeo de procesos · identificación del punto de partida ideal
            </p>
          </div>

          <!-- Flechas nav -->
          <div class="flex flex-col gap-2 flex-shrink-0">
            <button id="btn-prev" class="w-10 h-10 rounded-xl border border-white/10 bg-white/[0.03] hover:bg-white/[0.08] flex items-center justify-center transition-all" aria-label="Paso anterior">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M12 19l-7-7 7-7"/></svg>
            </button>
            <button id="btn-next" class="w-10 h-10 rounded-xl border border-white/10 bg-white/[0.03] hover:bg-white/[0.08] flex items-center justify-center transition-all" aria-label="Paso siguiente">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M12 5l7 7-7 7"/></svg>
            </button>
          </div>
        </div>

        <!-- Barra de progreso automática -->
        <div class="h-0.5 w-full" style="background:rgba(255,255,255,0.05);">
          <div id="auto-progress" class="h-full rounded-full" style="width:0%;"></div>
        </div>
      </div>

      <!-- Resultado -->
      <div class="flex justify-center mt-8">
        <div class="inline-flex items-center gap-3 px-6 py-3 rounded-2xl border"
             style="background:rgba(245,158,11,0.08);border-color:rgba(245,158,11,0.22);">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#F59E0B" stroke-width="2.5" stroke-linecap="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          <span class="text-sm font-semibold" style="color:#F59E0B;">Resultado: una operación integrada, medible y escalable</span>
        </div>
      </div>
    </div>

    <!-- ══ PRINCIPIOS ══ -->
    <div class="grid sm:grid-cols-3 gap-6 mt-24">
      {[
        { n:'01', title:'Implementación progresiva', desc:'Construimos primero un módulo útil y medible. Lo ampliamos con evidencia real, no con suposiciones.', color:'#6366F1' },
        { n:'02', title:'Transparencia total',       desc:'Cada decisión de arquitectura, seguridad e integración queda documentada y es completamente tuya.', color:'#8B5CF6' },
        { n:'03', title:'Acompañamiento continuo',   desc:'La solución evoluciona junto con los procesos y el equipo. No desaparecemos después del deploy.', color:'#EC4899' },
      ].map((p, i) => (
        <div class="glass rounded-2xl p-7 scroll-reveal group hover:-translate-y-1 transition-all duration-300" style={`transition-delay:${i*0.1}s;`}>
          <span class="inline-block w-10 h-10 rounded-xl flex items-center justify-center text-xs font-black font-mono mb-5 group-hover:scale-110 transition-transform"
                style={`background:${p.color}12;border:1px solid ${p.color}30;color:${p.color};`}>
            {p.n}
          </span>
          <h3 class="font-bold text-white mb-2 text-sm" style="font-family:'Plus Jakarta Sans',sans-serif;">{p.title}</h3>
          <p class="text-sm leading-relaxed" style="color:#94A3B8;">{p.desc}</p>
        </div>
      ))}
    </div>

  </div>
</section>

<style>
  @keyframes stepEnter {
    from { opacity: 0; transform: translateY(14px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  .step-content-anim { animation: stepEnter 0.45s cubic-bezier(0.16,1,0.3,1) both; }

  @keyframes progressRun {
    from { width: 0%; }
    to   { width: 100%; }
  }
  .progress-running {
    animation: progressRun var(--step-duration, 4s) linear forwards;
  }

  .step-btn .step-circle { cursor: pointer; }
  .step-btn.is-active .step-circle {
    transform: scale(1.12);
    border-width: 2px;
  }
  .step-btn.is-active .step-circle .step-ring { opacity: 1; animation: glowRing 1.8s ease-in-out infinite; }
  .step-btn.is-active .step-label { color: #F8FAFC; }
  .step-btn.is-done .step-label   { color: #64748B; }

  @keyframes glowRing {
    0%,100% { opacity: 0.7; transform: scale(1); }
    50%      { opacity: 1;   transform: scale(1.03); }
  }
</style>

<script define:vars={{ stepsJson }}>
  const steps = JSON.parse(stepsJson);
  const DURATION = 4200;
  let current = 0;
  let progressAnim = null;

  const trackProgress  = document.getElementById('track-progress');
  const panelGlow      = document.getElementById('panel-glow');
  const panelIconWrap  = document.getElementById('panel-icon-wrap');
  const panelIcon      = document.getElementById('panel-icon');
  const panelNum       = document.getElementById('panel-num');
  const panelTitle     = document.getElementById('panel-title');
  const panelDesc      = document.getElementById('panel-desc');
  const panelDetail    = document.getElementById('panel-detail');
  const autoProgress   = document.getElementById('auto-progress');
  const stepContent    = document.getElementById('step-content');
  const stepBtns       = document.querySelectorAll('.step-btn');
  const navDots        = document.querySelectorAll('.nav-dot');
  const btnPrev        = document.getElementById('btn-prev');
  const btnNext        = document.getElementById('btn-next');

  function activate(idx) {
    current = idx;
    const s = steps[idx];

    // Track line fill
    const pct = idx === 0 ? 0 : (idx / (steps.length - 1)) * 100;
    if (trackProgress) trackProgress.style.width = pct + '%';

    // Step circles
    stepBtns.forEach((btn, i) => {
      btn.classList.toggle('is-active', i === idx);
      btn.classList.toggle('is-done',   i < idx);
      const circle = btn.querySelector('.step-circle');
      const ring   = btn.querySelector('.step-ring');
      const label  = btn.querySelector('.step-label');
      if (!circle || !ring || !label) return;

      if (i === idx) {
        circle.style.background      = s.color + '22';
        circle.style.borderColor     = s.color;
        circle.style.boxShadow       = `0 0 0 4px ${s.color}20`;
        ring.style.opacity           = '1';
        label.style.color            = '#F8FAFC';
      } else if (i < idx) {
        circle.style.background      = steps[i].color + '18';
        circle.style.borderColor     = steps[i].color + '50';
        circle.style.boxShadow       = '';
        ring.style.opacity           = '0';
        label.style.color            = '#475569';
      } else {
        circle.style.background      = steps[i].color + '0A';
        circle.style.borderColor     = steps[i].color + '25';
        circle.style.boxShadow       = '';
        ring.style.opacity           = '0';
        label.style.color            = '#334155';
      }
    });

    // Nav dots
    navDots.forEach((dot, i) => {
      dot.style.width      = i === idx ? '20px' : '6px';
      dot.style.height     = '6px';
      dot.style.background = i === idx ? s.color : (i < idx ? s.color + '50' : 'rgba(255,255,255,0.12)');
      dot.style.borderRadius = '999px';
    });

    // Panel glow
    if (panelGlow) panelGlow.style.background =
      `radial-gradient(ellipse at 50% 0%,${s.color}28 0%,transparent 60%)`;

    // Panel icon
    if (panelIconWrap) {
      panelIconWrap.style.background   = s.color + '18';
      panelIconWrap.style.borderColor  = s.color + '50';
      panelIconWrap.style.boxShadow    = `0 0 30px ${s.color}20`;
    }
    if (panelIcon) {
      panelIcon.setAttribute('stroke', s.color);
      panelIcon.innerHTML = s.icon;
    }

    // Panel text — animate in
    if (stepContent) {
      stepContent.classList.remove('step-content-anim');
      void stepContent.offsetWidth; // reflow
      stepContent.classList.add('step-content-anim');
    }
    if (panelNum)    panelNum.textContent    = `PASO ${s.n} DE 07`;
    if (panelNum)    panelNum.style.color    = s.color;
    if (panelTitle)  panelTitle.textContent  = s.title;
    if (panelDesc)   panelDesc.textContent   = s.desc;
    if (panelDetail) { panelDetail.textContent = s.detail; panelDetail.style.color = s.color; }

    // Reset & run progress bar
    startProgress(s.color);
  }

  function startProgress(color) {
    if (!autoProgress) return;
    autoProgress.style.transition = 'none';
    autoProgress.style.width = '0%';
    autoProgress.style.background = color;
    autoProgress.classList.remove('progress-running');
    void autoProgress.offsetWidth;

    // Use CSS custom property + class for animation
    autoProgress.style.setProperty('--step-duration', DURATION + 'ms');
    autoProgress.classList.add('progress-running');

    clearTimeout(progressAnim);
    progressAnim = setTimeout(() => {
      const next = (current + 1) % steps.length;
      activate(next);
    }, DURATION);
  }

  // Controls
  btnPrev?.addEventListener('click', () => {
    clearTimeout(progressAnim);
    activate((current - 1 + steps.length) % steps.length);
  });
  btnNext?.addEventListener('click', () => {
    clearTimeout(progressAnim);
    activate((current + 1) % steps.length);
  });
  stepBtns.forEach((btn) => {
    btn.addEventListener('click', () => {
      clearTimeout(progressAnim);
      activate(Number(btn.dataset.idx));
    });
  });
  navDots.forEach((dot) => {
    dot.addEventListener('click', () => {
      clearTimeout(progressAnim);
      activate(Number(dot.dataset.idx));
    });
  });

  // Pause on hover
  const panel = document.getElementById('step-panel');
  panel?.addEventListener('mouseenter', () => { clearTimeout(progressAnim); autoProgress?.classList.remove('progress-running'); });
  panel?.addEventListener('mouseleave', () => startProgress(steps[current].color));

  // Start when visible
  const observer = new IntersectionObserver((entries) => {
    if (entries[0].isIntersecting) { activate(0); observer.disconnect(); }
  }, { threshold: 0.3 });
  const section = document.querySelector('section.py-28');
  if (section) observer.observe(section);
</script>
